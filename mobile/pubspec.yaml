name: st_marys_lms_mobile
description: ACK St. Mary's School Kabete - Parent & Teacher Mobile App
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.3.0 <4.0.0"
  flutter: ">=3.19.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6
  http: ^1.2.1
  provider: ^6.1.2
  shared_preferences: ^2.2.2
  intl: ^0.19.0
  go_router: ^13.2.0
  flutter_secure_storage: ^9.0.0
  jwt_decoder: ^2.0.1
  table_calendar: ^3.1.1
  fl_chart: ^0.67.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/logo.png
